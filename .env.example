# Environment Variables
NODE_ENV=production
PORT=3000

# Database Connection (Railway will provide this automatically)
DATABASE_URL=postgresql://user:password@host:port/database

# Optional: If you need to specify a different Gemini API endpoint
GEMINI_API_URL=https://ee-gemini-api-production.up.railway.app
